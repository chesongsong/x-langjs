{
  "name": "@x-lang/core",
  "version": "0.0.2",
  "description": "Core package for x-lang — parse, tokenize, and work with x-lang AST",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@x-lang/types": "workspace:*",
    "@x-lang/parser": "workspace:*",
    "@x-lang/ast": "workspace:*",
    "@x-lang/interpreter": "workspace:*",
    "@x-lang/render": "workspace:*",
    "antlr4ng": "^3.0.16"
  }
}
